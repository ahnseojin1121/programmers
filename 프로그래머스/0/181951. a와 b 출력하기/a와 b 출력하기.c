#include <stdio.h>

int main(void) {
    int a;
    int b;
    
    // 두 정수를 입력받기
    scanf("%d %d", &a, &b);
    
    // 입력받은 정수를 형식에 맞춰 출력하기
    printf("a = %d\n", a);
    printf("b = %d\n", b);
    
    return 0;
}